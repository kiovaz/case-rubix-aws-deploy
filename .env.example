# Configurações de Ambiente para Produção

# Porta do Nginx (porta exposta externamente)
NGINX_PORT=80

# URL da API para o frontend
# Em produção na AWS, use o domínio ou IP público
# Exemplo: NEXT_PUBLIC_API_URL=http://seu-dominio.com/api
NEXT_PUBLIC_API_URL=/api

# CORS Origin para o backend
# Em desenvolvimento: http://localhost
# Em produção: seu domínio ou * para permitir todos
CORS_ORIGIN=*
